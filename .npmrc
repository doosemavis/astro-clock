# Isolated (symlinked) linker so web (React 18) and mobile (React 19) each resolve
# their own co-located react/react-dom pair, instead of hoisting a mismatched
# react@19 + react-dom@18 to the root (which crashed Next's prerender).
node-linker=isolated
